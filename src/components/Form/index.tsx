import FormButton from "./FormButton";
import FormInput from "./FormInput";
import FormRoot from "./FormRoot";

const Form = {
    Root: FormRoot,
    Input: FormInput,
    Button: FormButton
}

export default Form;